/**
 * No longer used — sign-in is real (Supabase) as of the auth migration.
 * Kept as an empty component so any stray import doesn't break the build.
 * Safe to delete.
 */
export default function DemoBanner() {
  return null;
}
